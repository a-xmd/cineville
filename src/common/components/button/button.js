import classnames from 'classnames'
import styles from './button.module.scss'

const Button = (props) => {
  return <button className={classnames(styles.button)}>button tekst</button>
}

export default Button
